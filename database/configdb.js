/*var MongoClient = require('mongodb').MongoClient;

let connection = MongoClient.connect("mongodb://localhost:27017/bff", function (err, db) {
   
     if(err) throw err;

     console.log("Database connection established");
                
});

module.exports = {
    connection: connection
};

not workin

*/